<?php
$title = $pluralEntityName;
$this->headTitle($title);
?>
<h1><?php echo $this->escapeHtml($title); ?></h1>
<p><a href="<?php echo $this->url($route, array_merge($controllerRoute, array('action' => 'add'))); ?>">Add new <?= $entityName ?></a></p>
<table class="table table-bordered table-hover">
  <tr>
    <th><?= $entityName ?></th>
    <th>&nbsp;</th>
    <th>&nbsp;</th>
  </tr>
  <?php foreach (${$pluralEntityName} as $entity) : ?>
      <tr>
        <td><?php echo $this->escapeHtml($entity->getName()); ?></td>
        <td>
          <a href="<?php echo $this->url($route, array_merge($controllerRoute, array('action' => 'edit', 'id' => $entity->getId()))); ?>">Edit</a>
        </td>
        <td>
          <a href="<?php echo $this->url($route, array_merge($controllerRoute, array('action' => 'delete', 'id' => $entity->getId()))); ?>" onclick="return confirm('Are you sure?')">Delete</a>
        </td>
    </tr>
<?php endforeach; ?>
</table>